name: action
run-name: ${{ github.actor }} asking for main action

on:
  push:
    branches-ignore:
      - "ga-ignore-*"
  pull_request:
    branches-ignore:
      - "ga-ignore-*"

jobs:
  auto_format:
    name: Auto Format Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout GH repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Astyle
        run: sudo apt-get install -y astyle

      - name: Run Astyle
        run: |
          find . -name "*.cpp" -o -name "*.hpp" | xargs astyle --style=allman

  check_program_compilation:
    name: Check Program Compilation
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: auto_format
    steps:
      - name: Checkout GH repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies (SFML)
        run: |
          sudo apt-get update
          sudo apt-get install -y libsfml-dev

      - name: Remove build directory
        run: rm -rf build

      - name: Generate build files
        run: |
          cmake -S . -B build -DSFML_DIR="/usr/lib/x86_64-linux-gnu/cmake/SFML"

      - name: Compile project
        run: cmake --build build -- -j $(nproc --all)

      - name: Check executables
        run: |
          for file in ${{ vars.EXECUTABLES }}; do
            if [ ! -x "./build/${file}" ]; then
                echo "File $file does not exist or is not executable"
                exit 1
            fi
          done

  check_doc_build:
    name: Check Documentation Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout GH repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Clean CMake cache
        run: rm -rf CMakeCache.txt CMakeFiles Doxyfile *.cmake CMakeDoxyfile.in

      - name: Generate CMake files
        run: cmake .

      - name: Build documentation
        run: cmake --build . --target doc_doxygen || echo "No documentation target found, skipping."

  push_to_mirror:
    name: Push to Mirror
    runs-on: ubuntu-latest
    needs: [check_program_compilation, check_doc_build]
    if: github.event_name == 'push' && needs.check_program_compilation.result == 'success' && needs.check_doc_build.result == 'success'
    steps:
      - name: Checkout GH repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to mirror repository
        uses: pixta-dev/repository-mirroring-action@v1
        with:
          target_repo_url: ${{ vars.MIRROR_URL }}
          ssh_private_key: ${{ secrets.R_TYPE }}
